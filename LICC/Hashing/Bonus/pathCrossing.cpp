#include <iostream>
#include <vector>
#include <map>
#include <set>
using namespace std;

int main() {

    string path = "NESWW";
    map<int,int> points;
    currH = 0;
    currV = 0;

    for (int i = 0; i < path.size(); i++) {
        //create a map of coordinate pairs to see pairs visited
            //track all points and check if there are dupes at the end!
    }

    //if dupes are present, then return false
    //otherwise true

    return 0;
}
